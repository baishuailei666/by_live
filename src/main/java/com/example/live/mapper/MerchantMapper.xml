<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.live.mapper.MerchantMapper">

    <select id="merchantSearch" resultType="com.example.live.entity.Merchant">
        select id, mobile, shop
        from merchant
    </select>

    <select id="merchantList" resultType="com.example.live.entity.Merchant">
        select id, mobile, shop
        from merchant
        where 1=1
        and id in
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="getMerchantListByParams" resultType="com.example.live.entity.Merchant">
        SELECT id,mobile,shop,shop_id as shopId,shop_status as shopStatus
             ,ope_user as opeUser,days,login_count as loginCount,lt,ct
        FROM `merchant`
        where 1=1
          <if test="ids!=null and ids.size()!=0">
              and ope_user in
              <foreach collection="ids" item="item" index="index" open="(" separator="," close=")">
                  #{item}
              </foreach>
          </if>
        <if test="mobile !=null and mobile!=''">
            and mobile = #{mobile}
        </if>
        <if test="shop !=null and shop!=''">
            and shop LIKE CONCAT('%',#{shop},'%')
        </if>
        <if test="shopStatus !=null and shopStatus!=''">
          <if test="shopStatus='已认证'">
              and shop_status CONCAT('%',#{shop},'%')
          </if>

          <if test="shopStatus='已付费'">
              and shop_status CONCAT('%','卡','%')
          </if>

        </if>
        limit #{index}, 10
    </select>

    <select id="getMerchantListByParamsCount" resultType="int">
        SELECT count(1)
        FROM `merchant`
        where 1=1
        <if test="ids!=null and ids.size()!=0">
            and ope_user in
            <foreach collection="ids" item="item" index="index" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="mobile !=null and mobile!=''">
            and mobile = #{mobile}
        </if>
        <if test="shop !=null and shop!=''">
            and shop LIKE CONCAT('%',#{shop},'%')
        </if>
        <if test="shopStatus !=null and shopStatus!=''">
            <if test="shopStatus='已认证'">
                and shop_status CONCAT('%',#{shop},'%')
            </if>

            <if test="shopStatus='已付费'">
                and shop_status CONCAT('%','卡','%')
            </if>

        </if>
    </select>

</mapper>